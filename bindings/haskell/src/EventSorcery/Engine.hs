module EventSorcery.Engine (
  EngineError (..),
  ErrorClass (..),
  OpenOptions (..),
  Store,
  abiVersion,
  closeStore,
  decodeEngineError,
  encodeOpenOptions,
  openStore,
  supportsAbiVersion,
) where

import EventSorcery.Engine.Internal (
  EngineError (..),
  ErrorClass (..),
  OpenOptions (..),
  Store,
  abiVersion,
  closeStore,
  decodeEngineError,
  encodeOpenOptions,
  openStore,
  supportsAbiVersion,
 )

